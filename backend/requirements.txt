fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
python-multipart==0.0.9
aiofiles==23.2.1
pydantic==2.7.1
anthropic>=0.25.0
# OpenAI-compatible adapter — covers Groq, Gemini, Ollama, Azure, OpenRouter,
# and any other OpenAI-compatible service via custom base_url.
openai>=1.0.0
# File-ingest deps (drag-drop into Auto Generate)
pypdf==4.2.0
icalendar==5.0.12
# Scheduled jobs (lunchtime Overview refresh)
apscheduler==3.10.4